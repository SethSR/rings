
record Vec2 {
    x: s16,
    y: s16,
}

table Players[4] @ 0x0020_0000 {
		position: Vec2,
		velocity: Vec2,
}

proc test_table_access() {
    Players[0].position.x += Players[0].velocity.x;
    Players[0].position.y += Players[0].velocity.y;
}
